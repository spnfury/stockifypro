/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'app.stockify.pro',
      'images.unsplash.com',
      'upload.wikimedia.org',
    ],
  },
}

module.exports = nextConfig 